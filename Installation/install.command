#!/bin/bash
# macOS: double-click this file in Finder to install RSpace Interface.
# (It just runs install.sh in this same folder.)
cd "$(dirname "$0")"
bash ./install.sh
echo ""
read -rp "Press Enter to close this window..."
